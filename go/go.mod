module github.com/akzedevops/mmeqopendata/go

go 1.26

// Dependencies (cobra, go-retryablehttp, orb, kd-tree, x/sync/errgroup) are added
// during implementation — see ../specs/002-go-export-rewrite.md. The scaffold builds
// with the standard library only.
